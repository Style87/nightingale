<IfModule mod_rewrite.c>
	RewriteEngine on

  #RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization},L]

  RewriteRule .*public.* - [L,NC]
  RewriteRule ^(.*)$ index.php?q=$1&%{QUERY_STRING} [E=HTTP_AUTHORIZATION:%{HTTP:Authorization},L]
</IfModule>
